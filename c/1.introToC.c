#include <stdio.h> // standard input output header file. "#" is called a pre-processor

int main(){
	// this is a comment. A comment is ignored by the compiler
	printf("Hello, World!"); // printf function prints to screen. It is a function defined in stdio.h file
	return 0;
}
